# keep the published tarball clean — the `files` whitelist in package.json
# already scopes what ships; this just drops build/junk that lives inside it.
**/__pycache__/
*.pyc
*.pyo
.cairn/.beacon-sent
*.log
